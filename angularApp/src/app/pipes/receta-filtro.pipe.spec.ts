import { RecetaFiltroPipe } from './receta-filtro.pipe';

describe('RecetaFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new RecetaFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
